# Go bindings for Yahoo BOSS API

## How to use it

Install the library

    go get github.com/xiaowl/ygoss

There is an example to run

    go run examples/main.go -key=<consumer_key> -secret=<consumer_secret> [-search="text to search whole web"]
